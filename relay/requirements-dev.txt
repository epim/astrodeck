# Relay test deps. The off-wire unit suite needs only pytest (+ asyncio mode)
# and the runtime deps; the ON-WIRE integration test additionally needs httpx
# to drive a real browser request through a locally-launched relay+home.
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27

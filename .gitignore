__pycache__/
*.pyc
.venv/
node_modules/
dist/
ui/dist/
*.fits
captures/
.pytest_cache/
.remember/
*.egg-info/
server/config/
ui/tsconfig.tsbuildinfo
references/
native/target/

# external UI review evidence (referenced by docs/superpowers/reviews, kept local)
review-evidence*/

# Built SPA copied into the package for binary/wheel packaging (build_binary.py)
server/astrodeck/webui/

# Vendored ASTAP: fetched by scripts/fetch_astap.py at build time, never
# committed. The smallest usable star database is 102 MB, and a binary that
# large in git history is permanent, undiffable, and paid for by every clone.
# build_release.py --strict is what stops a release shipping without it.
/server/astrodeck/vendor/astap/
